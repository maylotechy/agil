import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder test — widget tests require a running backend and SMS permissions.
  // Integration tests should be run on a physical device.
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
